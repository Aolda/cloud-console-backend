gitlab:
  bot:
    url: ${GITLAB_URL}
    access-token: ${GITLAB_ACCESS_TOKEN}
    bot-username: ${GITLAB_BOT_USERNAME:claude}
    project-id: ${GITLAB_PROJECT_ID}

claude:
  api-key: ${CLAUDE_API_KEY}
  model: ${CLAUDE_MODEL:claude-sonnet-4-5-20250929}
  max-tokens: ${CLAUDE_MAX_TOKENS:4000}